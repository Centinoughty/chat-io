services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    command: npm start
    container_name: chat-client
    env_file:
      - ./env
    ports:
      - "3000:3000"
    networks:
      - app-network

networks:
  app-network:
    name: chatnet
    driver: bridge
